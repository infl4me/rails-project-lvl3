p = "Title: #{@bulletin.title}"
p = "Description: #{@bulletin.description}"
p = "Category: #{@bulletin.category.name}"
p = "User: #{@bulletin.user.formatted_name}"
p = image_tag @bulletin.image, class: 'img-thumbnail mb-2'

=> link_to 'Edit', edit_bulletin_path(@bulletin)
'|
=< link_to 'Back', bulletins_path
